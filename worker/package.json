{
  "name": "smart-food-delivery-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker REST API for Smart Food Delivery + IoT + ML Monitoring System",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "db:deploy": "tsx deploy-db.ts",
    "start": "tsx watch src/server.ts",
    "start:node": "tsx src/server.ts"
  },
  "dependencies": {
    "hono": "^4.7.2",
    "mysql2": "^3.12.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "tsx": "^4.19.3",
    "typescript": "^5.7.3",
    "wrangler": "^3.109.2"
  }
}
